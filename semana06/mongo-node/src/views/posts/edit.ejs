<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Post</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body class="grey lighten-4">

  <nav>
    <div class="nav-wrapper teal">
      <a href="/posts" class="brand-logo center">Social Media</a>
    </div>
  </nav>

  <div class="container">
    <h4 class="center-align">Editar Post</h4>

    <form action="/posts/<%= post._id %>?_method=PUT" method="POST" class="card-panel">
      <div class="input-field">
        <input type="text" name="title" id="title" value="<%= post.title %>" required minlength="5" maxlength="30">
        <label for="title" class="active">Título</label>
      </div>

      <div class="input-field">
        <textarea name="content" id="content" class="materialize-textarea" required minlength="10"><%= post.content %></textarea>
        <label for="content" class="active">Contenido</label>
      </div>

      <div class="input-field">
        <input type="text" name="hashtags" id="hashtags" value="<%= post.hashtags %>">
        <label for="hashtags" class="active">Hashtags</label>
      </div>

      <div class="input-field">
        <input type="text" name="imageUrl" id="imageUrl" value="<%= post.imageUrl %>">
        <label for="imageUrl" class="active">URL de la Imagen</label>
      </div>

      <% if (post.imageUrl) { %>
        <div class="center-align">
          <img src="<%= post.imageUrl %>" alt="Imagen del post" class="responsive-img z-depth-2" style="max-width:300px;">
        </div>
      <% } %>

      <button type="submit" class="btn waves-effect waves-light teal">
        <i class="material-icons left">save</i> Guardar Cambios
      </button>
    </form>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>
